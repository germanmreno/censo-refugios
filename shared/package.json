{
  "name": "shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./types/index.ts",
  "types": "./types/index.ts",
  "exports": {
    ".": "./types/index.ts",
    "./geo": "./geo/venezuela.json"
  },
  "scripts": {
    "build": "echo \"(shared: no build needed — consumed as .ts by backend/frontend via tsx/Vite)\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
